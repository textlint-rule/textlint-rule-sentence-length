// LICENSE : MIT
"use strict";
import sentenceSplitter from "sentence-splitter";
const defaultOptions = {
    max: 100
};
export default function (context, options = {}) {
    const maxLength = otpions.max || defaultOptions.max;
    // toPlainText
    return {
    }
}